specVersion: "v1"
serviceId: "demo-page"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "demo-page-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
